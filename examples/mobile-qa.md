# Example: Mobile QA

Invocation:

```text
/verify /mobile /playwright
```

Expected behavior:

- Capture mobile viewport evidence.
- Check text fit, overflow, CTA visibility, tap targets, sticky overlays, and reduced motion.
- Return Pass, Fail, or Needs Review.

