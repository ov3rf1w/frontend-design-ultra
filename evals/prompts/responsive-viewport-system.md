# Eval Prompt: Responsive Viewport System

Use `frontend-design-ultra`.

Request:

`/design /landing /responsive /premium /deep`

Create a first-viewport and section strategy for a premium homepage that must look intentional on compact laptop screens, standard desktop, large monitors, mobile Chrome, and reduced-motion mode. Include how type clamps, viewport height, media crop, CTA safe zones, wide-monitor staging, pinned sections, and screenshot verification should work.

