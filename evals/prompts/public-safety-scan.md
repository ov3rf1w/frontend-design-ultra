# Eval Prompt: Public Safety Scan

Use `frontend-design-ultra`.

Request:

`/verify /brutal`

Review a generated UI audit response before it is shared publicly. Check whether the response leaks private paths, confidential identifiers, copied-source claims, unverifiable metrics, or internal process names. Return a Pass/Fail/Needs Review decision and the exact safe rewrite strategy.

