# Eval Prompt: Editorial Motion System

Use `frontend-design-ultra`.

Request:

`/design /editorial /awwward /motion-high /gsap /deep`

Create an editorial landing interaction direction with scroll chapters, one signature motion move, reduced-motion fallback, mobile alternative, and implementation notes. It must not stack multiple competing effects.

