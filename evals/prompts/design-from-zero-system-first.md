# Eval Prompt: Design From Zero System First

Use `frontend-design-ultra`.

Request:

`/design /landing /premium /research /brandkit /deep`

Design a homepage direction for a new company with no brand guidelines. The result must not use default typography or generic blue/purple gradients. It must research the category, create a design system first, choose typography and color intentionally, define a signature move, and produce an implementation-ready UI spec.

